# Comma-separated btop HTTP API base URLs. Each entry may embed basic auth
# credentials as http://user:pass@host:port. Leave unset to serve from the same
# origin (default when btop --http serves the bundle itself, i.e. embedding).
VITE_BTOP_API_URL=http://127.0.0.1:8080
